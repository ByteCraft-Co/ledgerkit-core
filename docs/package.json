{
  "name": "ledgerkit-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },

  "devDependencies": {
    "vitepress": "1.3.3"
  },

  "overrides": {
    "esbuild": "0.25.0"
  },

  "engines": {
    "node": ">=18.0.0"
  }
}
